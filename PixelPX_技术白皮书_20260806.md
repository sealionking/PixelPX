# PixelPX — 从光变化中涌现 3D 结构

> 一种基于时序光场学习的图像生成新范式
>
> 版本：v1.0 公开技术白皮书
> 日期：2026-08-06
> 文档性质：知识产权宣示与技术方向公开

---

## 目录

1. [问题：Text-to-Image 的描述瓶颈](#1-问题text-to-image-的描述瓶颈)
2. [第一性原理：四条铁律](#2-第一性原理四条铁律)
3. [核心创新：从光变化中涌现 3D](#3-核心创新从光变化中涌现-3d)
4. [技术体系：PixelPX 架构](#4-技术体系pixelpx-架构)
5. [生成管线：text2sample-image2FINALIMAGE](#5-生成管线text2sample-image2finalimage)
6. [预训练成果](#6-预训练成果)
7. [与现有路线的本质区别](#7-与现有路线的本质区别)
8. [历史同构：400 年的画家早已验证](#8-历史同构400-年的画家早已验证)
9. [路线图](#9-路线图)

---

## 1. 问题：Text-to-Image 的描述瓶颈

当前 Text-to-Image（T2I）生成领域正陷入一场"卷参数、卷数据、卷算力"的军备竞赛。然而，一个根本性的瓶颈始终未被触及——**描述（Caption）本身**。

### 1.1 瓶颈的本质

每一张训练图像都必须通过"文本描述"才能被模型理解。描述成了文本与视觉信息之间的唯一接口。这个接口一旦存在歧义，就会变成隐性的"标签噪声"。

业界最新研究（2026 年 8 月）试图从两个维度优化这个问题：

- **训练端（Diffusability）**：用结构化条件（姿态、深度、分割）替代纯自然语言描述，在相同架构和预算下，评测分数从 56.2/76.1 提升到 72.5/85.2
- **推理端（Promptability）**：用智能体多轮反思重写提示词，将生成对齐度从 42.0% 提升到 54.0%

但这些努力本质上都是在**给瓶颈打补丁**。将最大重写轮数从 4 增加到 8，对齐度仅从 54.0% 微增至 54.7%——收益迅速递减。

### 1.2 我们的回答

**不要打补丁，端掉瓶颈。**

T2I 的根本问题不在于描述不够好，而在于**让文本承担了它不该承担的结构职责**。文本是语义的载体，不是几何的载体。让文本去描述"物体的 3D 形状如何随视角变化"，就像让音乐去描述颜色——跨域映射，必然失真。

PixelPX 的回答是：**把语义和结构彻底解耦。文本只管"画什么"，结构由模型从视频中自己学。**

---

## 2. 第一性原理：四条铁律

PixelPX 的全部设计源于四条第一性原理。它们不是优化技巧，而是范式选择。

### 铁律一：图片 = 静态视频 = 3D+时间的二维投影

图片不是"一张 2D 像素阵列"，而是**一个 3D 场景在某一时刻的一次快门投影**。

这意味着：如果你真正理解了 3D 场景的结构和光场，生成一张图片只需要"按下快门"——把 3D 场景投影到 2D 平面。不需要在 2D 潜空间里做概率拟合，不需要从噪声中去噪。

### 铁律二：用视频训练，不用图片

单张 2D 图片是"死"的——它只记录了某一瞬间的像素排列，丢失了深度、遮挡、光照方向等 3D 信息。

但视频包含时间维度。当自然光随时间变化时，同一个像素会记录到不同的光值。这些**光值的变化模式编码了 3D 信息**：朝阳面亮度变化剧烈，背阴面变化平缓，镜面反射高频闪烁，漫反射低频缓变。

视频教会模型 3D，就像多角度观察教会人类深度感。

### 铁律三：结构预测，而非像素生成

PixelPX 不直接生成像素。它先预测一个**3D 结构场**——每个像素位置对应的 3D 空间原点坐标——然后通过可微渲染将 3D 场投影成 2D 图像。

这区别于所有现有的扩散模型路线。扩散模型在 2D 像素空间或潜空间做去噪，本质上是在拟合 2D 概率分布。PixelPX 在 3D 结构空间做预测，然后投影——**生成的是"3D 世界的 2D 快照"，不是"2D 像素分布的采样"**。

### 铁律四：无物理引擎，只是"2D 模拟 3D 和时间"

这是最关键的一条，也是最容易被误解的一条。

PixelPX **没有物理引擎**。模型参数里没有流体力学方程，没有光线追迹算法，没有碰撞检测代码。3D 一致性不是"算出来"的，而是**从数据中学出来的统计几何先验**。

自然界已经替我们做了物理计算——流体力学让云飘动，天体力学让太阳移动。这些自然力把力的结果渲染成了光场变化。**我们只学光场模式，不建模力本身。**

这意味着 PixelPX 不会因为缺少物理公式而崩溃，也不会因为外挂伪物理约束（如硬编码重力参数）而产生"伪物理"。3D 一致性是通过几何约束注意力机制和等变性损失，从海量视频数据中**涌现**出来的。

---

## 3. 核心创新：从光变化中涌现 3D

### 3.1 3D 信号的来源

传统 3D 视觉依赖两种信号源：

| 信号源 | 原理 | 问题 |
|---|---|---|
| **视差**（相机运动） | 不同视角的像素位移编码深度 | 需要多相机或移动拍摄，数据采集昂贵 |
| **动态**（物体运动） | 物体运动轨迹揭示 3D 结构 | 需要建模生物力学/机械力，超出当前能力 |

PixelPX 发现了**第三种信号源：自然光变化**。

### 3.2 自然力做物理，我们只学光场

固定机位下，每个像素 (h,w) 对应固定的 3D 表面点。当自然光随时间变化时，不同像素记录到不同的光值序列。这个**差异化光响应编码了 3D**：

| 表面属性 | 光响应模式 | 3D 含义 |
|---|---|---|
| 朝阳面 | 亮度随光向变化剧烈 | 法线朝向光源 |
| 背阴面 | 亮度低且变化缓 | 法线背向光源 |
| 镜面反射 | 高频闪烁 | 光滑材质 |
| 漫反射 | 低频缓变 | 粗糙材质 |
| 遮挡边界 | 光响应突变 | 深度不连续 |

**关键洞察**：自然力（流体 + 天体）已经替我们完成了物理计算，把结果渲染成了光场变化。我们不需要建模力本身，只需要学光场的统计模式。这就是"无物理引擎"路线的真正含义。

### 3.3 控制变量法：固定机位

PixelPX 的训练数据有一个铁律：**固定机位**。

这不是技术限制，而是**实验设计**。固定机位意味着 3D 几何不变，唯一变化的变量是光照/时间。模型被迫分离"不变的 3D 结构"和"变化的光场"——这就是解耦。

| 变量 | 是否变化 | 模型学到 |
|---|---|---|
| 3D 几何 | 不变 | 静态 3D 结构场 |
| 光照/时间 | 变化 | 动态光场流形 |
| 天气/纹理 | 局部变化 | 外观分布 |

如果允许相机运动，3D 几何和光场变化会纠缠在一起，模型无法分离两者。固定机位是控制变量法的工程体现。

### 3.4 时序帧栈：光变化的"多视角"

PixelPX 将同一机位的 K 帧观测定义为**时序帧栈（Temporal Frame Stack, TFS）**。TFS 的每一帧不是空间上的不同视角，而是**时间上的不同光场采样**。

- 高帧率（密采样）→ 光场约束强 → 3D 预测准
- 低帧率（稀疏采样）→ 光场约束弱 → 需场景先验补偿

帧栈深度 K 是 PixelPX 的核心超参数之一，直接控制 3D 预测的信息量。

---

## 4. 技术体系：PixelPX 架构

### 4.1 命名体系

PixelPX 构建了完全自有的技术命名体系，不依赖任何外部算法的术语。

| PixelPX 术语 | 缩写 | 含义 |
|---|---|---|
| **PXField** | PXF | 3D 原点场：每像素的 3D 空间原点坐标 |
| **ChromaField** | C | 外观场：纹理/色彩特征 |
| **LuxField** | L(t) | 光场：随时间变化的光照参数 |
| **RelationalField** | RF | 关系场：patch 间的几何关系 |
| **Temporal Frame Stack** | TFS | 时序帧栈：同一机位的 K 帧观测 |
| **PXFormer** | — | 核心推理引擎 |
| **GeoTriangle** | GT | 几何三角约束注意力 |
| **GXI** | — | 几何不变性注意力 |
| **PXScene** | — | 场景模块（输出 3D 结构） |
| **PXLoop** | — | 循环精化机制 |
| **PXscore** | PXS | 每 patch 的 3D 预测置信度 |
| **PXDistill** | — | 自蒸馏机制 |

### 4.2 五阶段前向架构

```
输入：视频 V = [I₁..I_K]（K 帧固定机位观测）
      （可选）文本 T（辅助语义条件，非主条件）

  ┌─────────────────────────────────────┐
  │ Stage 1 · TFS 编码                   │
  │   patch 化 + 时序帧栈构建             │
  └──────────────┬──────────────────────┘
                 ↓
  ┌─────────────────────────────────────┐
  │ Stage 2 · PXFormer（L=24~48 块）     │
  │   空间注意力 + 时序注意力             │
  │   + 几何三角约束 + 帧栈↔关系场通信    │
  └──────────────┬──────────────────────┘
                 ↓
  ┌─────────────────────────────────────┐
  │ Stage 3 · PXScene（GXI 不变性）      │
  │   几何不变性注意力，输出 3D 原点场     │
  └──────────────┬──────────────────────┘
                 ↓
  ┌─────────────────────────────────────┐
  │ Stage 4 · 可微渲染器                  │
  │   3D 原点 + 外观 → 2D 图像            │
  └──────────────┬──────────────────────┘
                 ↓
  ┌─────────────────────────────────────┐
  │ Stage 5 · PXLoop（R=3~4 轮）          │
  │   3D 原点场反馈精化                   │
  └─────────────────────────────────────┘

输出：2D 图像 Î ∈ R^{3×H×W}
```

### 4.3 核心模块详解

#### PXFormer：时空联合推理

PXFormer 是 PixelPX 的核心推理引擎，每块含四个子模块：

1. **空间注意力（SA）**：帧内 patch 间的自注意力，学习同一时刻的空间结构关系
2. **时序注意力（TA）**：帧间 patch 对应的跨帧注意力——这是"视频教 3D"的核心：找哪些 patch 跨帧追溯同一 3D 表面点
3. **几何三角约束（GeoTriangle）**：对关系场施加三角不等式约束，强制几何一致性——3D 空间中三个点的距离必须满足三角不等式，这是非物理的纯几何先验
4. **帧栈↔关系场双向通信**：帧栈信息注入关系场（外积均值），关系场信息反馈帧栈注意力（关系偏置）

#### GXI：几何不变性注意力

GXI（Geometric X Invariance）是 PXScene 的核心算子。它保证：**在全局相机变换（旋转、平移）下，3D 原点坐标随之等变，但特征表示不变**。

这意味着模型学到的 3D 结构是真正以场景为中心的，而不是以相机为中心的。换一个视角看同一个场景，3D 结构场不变，只是投影变了。

GXI 是 PixelPX 最关键的创新之一——它让模型从"以相机为中心的 2D 像素拟合"升级为"以场景为中心的 3D 结构预测"。

#### PXLoop：循环精化

PXLoop 将 3D 原点场的预测结果反馈到 PXFormer 和 PXScene 的输入，进行 R 轮（3~4 轮）迭代精化。每一轮利用上一轮的 3D 预测作为先验，逐步收紧几何约束。

#### PXDistill：自蒸馏

PXDistill 让模型用自身预测的 3D 结构场作为伪标签，扩展训练集。每轮自蒸馏收紧 3D 先验，形成正向循环。这使得 PixelPX 的训练数据可以无限扩展——只要有视频，就能自举。

### 4.4 3D 表征：PXField

每个 patch i 输出：

| 表征 | 符号 | 维度 | 含义 |
|---|---|---|---|
| 3D 原点 | X_i | R³ | 该 patch 中心像素的光在 3D 世界的发出点 |
| 外观特征 | f_i | R^{d_app} | RGB / 纹理特征 |
| 不透明度 | α_i | R | 渲染合成用 |

**3D 原点是 PixelPX 的核心预测目标**。它不是深度图（深度是相机坐标系下的 Z 值），而是**3D 空间中的绝对坐标**——换了相机，原点不变，投影变。

### 4.5 损失函数体系

| 损失 | 名称 | 作用 |
|---|---|---|
| **PXRecon** | 重建损失 | 多帧重投影重建 |
| **PXInterpol** | 插时刻损失 | 预测未观测时刻的帧——**命门指标**：通过 = 光场学到 = 3D 学到 |
| **PXEquiv** | 等变损失 | 相机变换下 3D 原点场等变 |
| **PXAlign** | 对齐损失 | 3D 原点场的几何对齐 |
| **PXSmooth** | 平滑损失 | 光场时间连续性 |

**无物理损失**。全部是统计 + 几何先验。

---

## 5. 生成管线：text2sample-image2FINALIMAGE

PixelPX 采用三段式生成管线，将语义生成与结构预测彻底解耦。

```
text ──[Stage 1: text2sample]──→ sample image
                                     │
                                     ↓
                              [Stage 2: image2structure]
                                     │
                                     ↓
                              3D 原点场（PXField）
                                     │
                                     ↓
                              [Stage 3: structure2FINALIMAGE]
                                     │
                                     ↓
                              FINAL IMAGE
```

### Stage 1 · text2sample（语义生成）

将用户的文本意图转为一张 2D 草图。**只管语义，不管结构**。

- 直接调用现成 T2I 模型（FLUX / Midjourney / SDXL 等）
- 文本只负责"画什么"，不负责"结构对不对"
- 这一步不需要训练——白嫖 T2I 生态

### Stage 2 · image2structure（结构抬升）

将草图抬升为 3D 原点场。**这是 PixelPX 的核心**。

- 草图 → TFS 编码 → PXFormer + PXScene → 3D 原点场
- 单图相当于"浅帧栈"（K=1），约束弱，用场景先验补偿
- 场景先验检索：从训练库中找相似场景的 3D 模板，作"虚拟多帧"

### Stage 3 · structure2FINALIMAGE（渲染成片）

将 3D 原点场渲染成最终 2D 图像。

- 可微渲染：3D 原点 + 外观 → 2D 投影
- 扩散精化器（可选）：在渲染图上采样外观细节（纹理、云、随机细节），但**不改变 3D 结构**

### 三段式的优势

| 优势 | 说明 |
|---|---|
| **caption 瓶颈消失** | 文本只管语义，结构不依赖文本描述 |
| **3D 一致性** | Stage 2 保证结构对，破 T2I 的物理崩坏 |
| **可解释** | 3D 原点场是显式中间表征，可调试 |
| **可编辑** | 改 3D 原点场 → 改图，比改 latent 直观 |
| **可导航** | 同一 3D 原点场可渲染多视角/多时刻 |

---

## 6. 预训练成果

PixelPX 已完成 6 个训练 Step 的渐进式验证，从合成数据到真实多场景，从灰度到彩色，从重建到补全。所有训练在单卡 RTX 3050 Laptop GPU（4GB 显存）上完成。

### 6.1 训练路线

| Step | 内容 | 关键指标 | 状态 |
|---|---|---|---|
| Step 1 | 合成数据最小验证 | 重建 L1=0.040，插时刻=0.040 | ✅ |
| Step 2 | 3D 原点场 + 几何不变性验证 | Z 轴相关性 0.9997 | ✅ |
| Step 3 | 真实牧场单场景 | 重建 L1=0.035 | ✅ |
| Step 4 | 多场景灰度（5 场景） | 重建 L1=0.058 | ✅ |
| Step 5 | RGB 彩色（5 场景） | 重建 L1=0.067，光场捕获色温 | ✅ |
| 14 机位 | SkyFinder 14 机位联合训练 | 重建 L1=0.0313，插时刻=0.0320 | ✅ |
| Step 6 | BERT 式 Mask 补全训练 | 补全/重建比值 0.86 | ✅ |
| De Novo | 从头生成测试 | 30 张抽卡，多样性 2.75 | ✅ |

### 6.2 核心指标解读

#### 重建 L1 与插时刻 L1

重建 L1 衡量模型对已观测帧的重建精度。插时刻 L1 衡量模型对**未观测时刻**帧的预测精度。

**插时刻 L1 ≈ 重建 L1 是命门**：如果模型只是"死记像素"，插时刻误差会远高于重建误差。插时刻 ≈ 重建意味着模型学到了"3D 结构 + 光场变化规律"，能从已观测时刻的光场推断未观测时刻的画面。

14 机位训练结果：重建 L1=0.0313，插时刻 L1=0.0320，比值 1.02——**接近完美**。

#### 补全/重建比值

Step 6 的 BERT 式 Mask 训练引入了随机遮盖 0~50% patch 的策略。补全/重建比值衡量"遮盖 50% 后补全的误差"与"完整输入重建的误差"之比。

| 比值 | 含义 |
|---|---|
| < 1 | 模型学到了场景先验，能从部分推断整体 |
| = 1 | 模型对遮盖不敏感 |
| > 1 | 模型依赖完整输入，没学到补全 |

**Step 6 结果**：比值从训练前的 10.6（完全失败）降到 0.86（成功）。**补全比完整重建还准 14%**——模型不是"复制粘贴"，而是学到了"场景通用结构先验"。

#### 光场捕获色温

Step 5 的 RGB 训练中，光场幅度从灰度的 7~15 扩展到彩色的 11~22，增幅 50~100%。这表明模型从色彩中提取了**比亮度更丰富的光场信息**——包括色温变化。

### 6.3 多场景泛化证据

Step 4 的训练中包含一个独特的对照实验：同一机位的"盖前"和"盖后"场景。新建筑盖起后，3D 结构变了，光响应模式就变了：

| 区域 | 盖前 | 盖后 | 含义 |
|---|---|---|---|
| 天空 | std=5.62 | std=1.14 | 新建筑挡住天空视野 |
| 建筑 | std=7.62 | std=6.40 | 新结构产生不同阴影 |
| 地面 | std=3.80 | std=1.40 | 地面被遮挡 |

**同一机位，3D 结构变 → 光响应模式变**。这直接验证了"结构决定光响应"的核心假设。

14 机位训练进一步验证：14 场景比 5 场景重建 L1 下降 53%（0.067→0.0313），**帧栈深度扩展有效**。

### 6.4 模型规模

当前预训练模型仅 1.01M 参数，在 4GB 显存的消费级 GPU 上完成全部训练。这不是最终规模——而是**架构验证的最小可行实现**。

验证成功后，扩展路径明确：参数 1M→100M、场景 14→1000+、帧栈深度 K=8→16+。

---

## 7. 与现有路线的本质区别

### 7.1 对比矩阵

| 维度 | T2I 扩散模型 | 伪世界模型视频生成 | 3D 世界生成派 | **PixelPX** |
|---|---|---|---|---|
| **输入** | 文本 | 文本 | 图像（前置 T2I） | 图像（前置 T2I） |
| **输出** | 2D 图像 | 视频 | 3D 世界 | 2D 图像（3D 投影） |
| **3D 表征** | 无 | 无显式 | 渲染基元 | **3D 原点场** |
| **一致性机制** | 无 | 隐式（概率） | 渲染基元几何 | **几何三角约束 + 不变性 + 等变** |
| **物理引擎** | 无 | 伪物理（外挂损失） | 无 | 无（几何先验学出来） |
| **训练数据** | 图文对 | 海量视频 | 配对 (image, 3D) | **单视频自监督** |
| **文本角色** | 主条件 | 主条件 | 前置 T2I | 前置 T2I（可选） |
| **caption 瓶颈** | 有 | 有 | 无 | **无** |

### 7.2 核心差异

#### 与 T2I 扩散模型的区别

T2I 在 2D 潜空间做概率拟合，模型不知道"苹果"是 3D 球体，只知道"红色圆形像素块"旁边大概率跟着"高光像素块"。一旦遇到复杂透视或罕见物理交互，统计数据不足就崩溃。

PixelPX 先预测 3D 结构再投影，**结构由几何先验保证，不依赖统计概率**。

#### 与伪世界模型视频生成的区别

当前的视频生成模型（如 Sora 类系统）本质上仍是 2D+时间的像素扩散。它们的"物理"是外挂损失项——把重力参数硬塞进统计模型，是打补丁不是建模型。

PixelPX 不外挂任何物理公式。3D 一致性从几何三角约束和等变性中**涌现**。

#### 与 3D 世界生成派的区别

3D 世界生成派（如 World Labs Marble）需要配对 (image, 3D) 数据训练，数据采集成本极高。3D 数据稀缺是其命门。

PixelPX 用**单视频自监督**训练——视频帧本身就是真值，不需要 3D 标注。视频数据无限，只愁算力。**算力可以买，数据买不到。**

### 7.3 数据效率优势

| 维度 | 3D 世界生成派 | PixelPX |
|---|---|---|
| 训练数据类型 | 配对 (image, 3D) | 单视频 |
| 数据获取成本 | 极高 | 极低 |
| 数据规模上限 | 百万级（受 3D 采集限制） | 亿级（互联网无限） |
| 自监督 | 需要 3D 真值 | 视频帧本身是真值 |
| 自蒸馏扩数据 | 无 3D 真值无法自蒸馏 | PXDistill 无限扩数据 |

---

## 8. 历史同构：400 年的画家早已验证

PixelPX 的方法论不是空中楼阁——**400 年的古典画家实践就是本路线的历史验证**。

### 8.1 画家透镜法 ↔ PixelPX

| 画家透镜法 | PixelPX |
|---|---|
| 暗箱（camera obscura）固定投影 | 固定机位相机 |
| 透镜把 3D 场景投影到 2D 画布 | 相机把 3D 场景投影到 2D 像素 |
| 长时间坐姿，太阳在动，阴影在变 | 长时间拍摄，自然光在变 |
| 画家做"光校正"：分离物体本身和此刻的光 | 模型解耦静态 3D 原点场与动态光场 |
| 画家从光变化脑补 3D 几何 | 模型从光响应差异涌现 3D 表征 |
| 落笔一帧 = 理解完整光场后的一次快门 | 渲染输出 = 学会光场后的一次投影 |

### 8.2 光校正不是补偿，是理解

以维米尔画《倒牛奶的女仆》为例：坐几小时，太阳从东窗移到西窗，桌面阴影一直在变。画家必须从"阴影怎么变"推断出"桌面是平的、罐子是圆的、墙面是凹的"。**他就是从光变化学 3D**。

然后落笔那一刻，画的是"理解了完整光场之后的一个时刻"，不是"抄了某一瞬间的像素"。

这与 PixelPX 完全同构：学光场 → 涌现 3D 原点场 → 渲染任一时刻的静态帧。

### 8.3 "无物理引擎"的历史印证

画家从来不建模光学物理：

- 不计算光线追迹方程
- 不解流体力学（云怎么飘）
- 不解天体力学（太阳怎么走）
- 只**观察**自然力渲染好的光场模式，**学**这个模式

**让自然做物理，我们只学光场。** 画家做了 400 年，PixelPX 把画家的脑子换成 PXFormer + GXI。

---

## 9. 路线图

### 9.1 已完成（Pre-phase）

- ✅ 架构证伪：从合成数据到真实多场景，3D 原点场从自监督中涌现
- ✅ 多场景泛化：14 机位联合训练，帧栈深度扩展有效
- ✅ RGB 彩色：光场捕获色温，三通道均衡学习
- ✅ 补全能力：BERT 式 Mask 训练，模型从"重建器"升级为"补全器"
- ✅ 从头生成：De Novo 模式验证，30 张抽卡多样性 2.75

### 9.2 进行中

| 阶段 | 目标 | 状态 |
|---|---|---|
| 全链条定锚 | 算子/训练/生成三层架构审计 | 进行中 |
| Phase 1 训练 | 扩规模到 100 场景，接入完整算子 | 待启动 |
| Phase 2 训练 | PXscore 打分体系（per-patch 置信度 + 全局分） | 待启动 |
| Phase 3 训练 | 完整 GXI 等变模块 + PXLoop 循环精化 | 待启动 |
| Phase 4 训练 | De Novo 质量提升 + 扩散精化器接入 | 待启动 |

### 9.3 产品形态

PixelPX 的产品逻辑：

```
视频 = 训练数据（学光影关系）
  ↓ 降维打击
图片 = 应用产品（补全/生成）
```

三种生成模式：

| 模式 | 输入 | 输出 | 场景 |
|---|---|---|---|
| **De Novo**（从头设计） | 无（或仅种子） | 全新图像 | 抽卡生成 |
| **Completion**（补全） | 部分图像 | 完整图像 | 图像修复/扩展 |
| **Transformation**（变换） | 完整图像 + 目标光场 | 变换后图像 | 换光/换时刻 |

---

## 结语

PixelPX 不是在 T2I 的地基上打补丁，而是**换了一个地基**。

从"2D 像素概率拟合"到"3D 结构预测 + 投影"，从"文本描述瓶颈"到"语义与结构解耦"，从"外挂伪物理"到"从光变化中涌现几何先验"——这是一次范式级别的跃迁。

400 年前，画家用透镜和耐心从光变化中学会 3D。今天，PixelPX 用时序帧栈和几何不变性做同样的事。方法论同构，工具升级。

**PixelPX：从光变化中涌现 3D 结构。**

---

*本文档为 PixelPX 技术方向的公开宣示。具体算子实现、训练细节和架构参数属于商业机密，本文不予披露。*
